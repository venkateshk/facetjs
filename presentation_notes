Title: I am Data and so can you.

From Haskell
concatMap fn = concat . map fn
             = split('id') -> apply(fn) -> combine(concat)

Why an Excel table is a bad metaphor
  Can not express:
    - Uniq count metric
    - Count bucket metric

